name: campus_gemini_2
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  # Core
  provider: ^6.0.0
  http: ^1.1.0
  shared_preferences: ^2.2.0
  flutter_svg: ^2.0.7

  # Firebase (Required for Auth Persistence & Token Exchange)
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0

  # UI
  google_fonts: ^6.1.0
  intl: ^0.20.2
  camera: ^0.11.3
  permission_handler: ^12.0.1
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.svg
